# SakuttoSeat 実機動作確認チェックリスト

**作成日**: 2026年8月11日  
**対象バージョン**: リファクタ後（API名変更、Interactor責務集約版以降）  
**テスト環境**: 実機 iPhone  
**実行者**: ___________

---

## テスト実施前の確認

- [ ] 実機にアプリケーションがインストール済みであることを確認
- [ ] 実機が最新の iOS バージョンであることを確認（推奨: iOS 18.2 以上）
- [ ] インターネット接続が確保されていることを確認（広告読み込み用）
- [ ] アプリケーションにデータがない状態から開始する（キャッシュ削除済み）
- [ ] テスト環境で十分な時間が取れることを確認（推定: 30～45分）

---

## 1. 初期起動 & UIレイアウト確認

### 1.1 アプリケーション起動
- [ ] アプリケーションが正常に起動する
- [ ] 参加者追加画面（AttendeeListView）が表示される
- [ ] ナビゲーションバーが正しく表示される（"サクッと席決め"的なタイトル）
- [ ] 広告バナーが下部に表示される（グレースフルに失敗しても OK）

### 1.2 初期状態の UI 確認
- [ ] 参加者リストが空の場合、"参加者を追加してください" と表示される
- [ ] 入力欄（TextField）が フォーカスされていない状態で表示される
- [ ] 各ボタン（追加・シャッフル・保存・一括ペースト・リセット）が正しく配置されている
- [ ] リセット・保存ボタンは disabled 状態（グレーアウト）である

---

## 2. 参加者追加機能 (基本的な入力・追加)

### 2.1 単一参加者追加
- [ ] TextField に名前を入力して「+」ボタンをタップすると、リストに追加される
  - テスト: "田中" を入力して追加
- [ ] 入力欄がクリアされ、フォーカスが戻る（連続入力可能）
- [ ] 追加された参加者が番号（1, 2, 3...）と共に表示される
- [ ] 参加者の表示順序が挿入順と一致する

### 2.2 whitespace のトリミング
- [ ] 前後に空白がある名前を入力しても、末尾の空白が除去されて保存される
  - テスト: "  佐藤  " を入力 → "佐藤" として保存されることを確認
- [ ] タブや改行を含む入力も同様にトリミングされる
  - テスト: "太郎\n" を入力 → "太郎" として保存

### 2.3 空文字の入力ハンドリング
- [ ] 空文字（または空白のみ）を入力して追加ボタンをタップしても何も起こらない
  - テスト: "   " だけを入力して追加
- [ ] エラーメッセージは表示されない（無視される）

### 2.4 同一名前の自動リネーム
- [ ] 同じ名前を複数回追加すると、2番目以降は末尾に "（2）"、"（3）" が付与される
  - テスト: "山田" を3回追加 → "山田", "山田(2)", "山田(3)" と表示される
- [ ] リネームの連番は正しく増加する
- [ ] 既に "山田(2)" が存在する場合でも、新しく追加される "山田" には "山田(2)" が付いて、前のものとは被らない

### 2.5 キーボード submit の動作
- [ ] TextField で return キーを押しても参加者が追加される（ボタンと同じ動作）
- [ ] submit 後に入力欄がクリアされ、キーボードが隠れる（またはフォーカスが継続して再表示）

---

## 3. 参加者削除機能

### 3.1 スワイプ削除
- [ ] リスト内の参加者を左にスワイプすると、削除アイコンが表示される
- [ ] 削除ボタンをタップすると、その参加者がリストから削除される
- [ ] 削除後に番号の順序が自動更新される（例: 3項目が [1번, 2번, 3번] に戻る）

### 3.2 複数削除（EditMode）
- [ ] ナビゲーションバーの「Edit」ボタンをタップすると、各行に丸いチェックマークが表示される
- [ ] 複数の参加者をチェックして「Delete」をタップすると、一括削除される
- [ ] 削除後に「Done」をタップして Edit モードを終了する

### 3.3 削除後の UI 更新
- [ ] すべての参加者を削除すると、再び "参加者を追加してください" メッセージが表示される
- [ ] リセット・保存ボタンが disabled に戻る

---

## 4. 全削除（リセット）機能

### 4.1 リセット操作
- [ ] 参加者が存在する場合、ナビゲーションバー右上の「trash」アイコン（リセット）が有効（濃い色）である
- [ ] リセットアイコンをタップするとアラートが表示される
- [ ] アラートで「削除」をタップすると、すべての参加者が削除される
- [ ] アラートで「キャンセル」をタップすると、何も削除されない

### 4.2 リセット後の状態
- [ ] リセット後、参加者リストが完全にクリアされる
- [ ] カウントが 0 にリセットされる
- [ ] "参加者を追加してください" メッセージが再表示される

---

## 5. シャッフル機能

### 5.1 シャッフル実行
- [ ] 参加者が 2名以上いる場合、下部の「shuffle」ボタンが有効である
- [ ] シャッフルボタンをタップすると、参加者の順序がランダムに並び替わる
- [ ] 参加者の番号（1, 2, 3...）は順序に合わせて自動更新される

### 5.2 シャッフル結果の変動性
- [ ] 同じメンバーでシャッフルを複数回実行しても、毎回異なる順序になる（ほぼ）
  - テスト: 5人の参加者で3回以上シャッフルして、異なる順序が出ることを確認
- [ ] 稀に同じ順序が出る可能性がある（仕様で最大3回リトライしている）ことを理解

### 5.3 シャッフルボタンの有効・無効
- [ ] 参加者が 0 名の場合、シャッフルボタンは disabled（グレーアウト）である
- [ ] 参加者が 1 名の場合、シャッフルボタンは enabled だが、実行しても順序は変わらない
  - テスト: 1人だけの場合にシャッフルをタップして、順序が変わらないことを確認

---

## 6. 一括追加機能（Bulk Add）

### 6.1 一括追加シート表示
- [ ] ナビゲーションバー右上の「clipboard」アイコンをタップすると、シート画面が表示される
- [ ] シート内に大きなテキスト入力領域があり、複数行入力可能である

### 6.2 改行での分割
- [ ] 複数の名前を改行で区切り、「追加」ボタンをタップすると、各行が1人の参加者として追加される
  - テスト入力:
    ```
    山田
    鈴木
    高橋
    ```
  - 期待: 3人が順番に追加される

### 6.3 カンマでの分割
- [ ] カンマで区切った名前も同様に分割されて追加される
  - テスト入力: `田中,佐藤,伊藤` → 3人が追加される
- [ ] 改行とカンマが混在していても正しく分割される
  - テスト入力:
    ```
    田中,佐藤
    伊藤,中村
    ```

### 6.4 全角句点での分割
- [ ] 全角句点（、）で区切った名前も分割される
  - テスト入力: `田中、佐藤、伊藤` → 3人が追加される

### 6.5 whitespace トリミング
- [ ] 各名前の前後の空白/改行は自動的に除去される
  - テスト入力: `  田中  ,   佐藤   ` → "田中", "佐藤" として追加

### 6.6 空行・空文字の無視
- [ ] 空行や空文字のみの行は無視される
  - テスト入力:
    ```
    田中
    
    佐藤
    ```
  - 期待: 2人が追加される（空行は無視）

### 6.7 自動リネーム（一括追加時）
- [ ] 一括追加で同一名前が複数含まれている場合も、自動的にリネームされる
  - テスト入力: `山田,山田,山田` → "山田", "山田(2)", "山田(3)" として追加

### 6.8 シート閉じる
- [ ] "完了" または戻るジェスチャーでシートを閉じれば、追加された参加者が表示される

---

## 7. お気に入りグループ機能（GroupFavorite）

### 7.1 グループ保存
- [ ] 参加者が複数いる場合、ナビゲーションバー右上の「save」ボタンが有効である
- [ ] 保存ボタンをタップすると、テキスト入力アラートが表示される
- [ ] グループ名を入力して「保存」をタップする
  - テスト: グループ名 "チームA" を入力

### 7.2 グループ数の制限
- [ ] グループを3つまで保存できる
- [ ] 4つ目を保存しようとするとアラートが表示される: "最大3つまでのグループを保存できます"
- [ ] 4つ目の保存は キャンセルされる

### 7.3 グループ読み込み（ロード）
- [ ] "favorite" アイコン（ハート形）をタップすると、保存されているグループ一覧がシートで表示される
- [ ] グループ名をタップするとそのグループが読み込まれる
  - テスト: "チームA" をタップ → 現在の参加者リストが "チームA" の内容に置き換わる
- [ ] 読み込みの際は既存の参加者リストはクリアされる

### 7.4 グループ削除
- [ ] グループ一覧シート内で グループを左スワイプ削除できる
- [ ] または、EditMode で複数削除できる
- [ ] 削除後は一覧から消える

### 7.5 グループ機能の UI 動作
- [ ] グループ一覧シートのナビゲーションバーに「編集」と「閉じる」ボタンがある
- [ ] グループがない場合、グループアイコンは disabled（グレーアウト）である

---

## 8. 座席表（SeatingChart）機能

### 8.1 座席表画面への遷移
- [ ] 参加者が複数いる場合、下部に「座席表」ボタンが表示される
- [ ] ボタンをタップすると座席表画面に遷移する
- [ ] 参加者がいない場合、ボタンは表示されない（または disabled）

### 8.2 テーブル自動配置
- [ ] 座席表画面に遷移したときに、参加者が自動的にテーブル（席）に配置される
- [ ] テーブルは複数表示される（参加者数に応じて）
- [ ] テーブル上部にテーブル名（例："テーブル1", "テーブル2"）が表示される

### 8.3 テーブル追加ボタン
- [ ] グリッド下部に「テーブル追加」ボタンが表示される
- [ ] ボタンをタップすると、新しい空のテーブルが追加される
- [ ] テーブルの最大数制限がある場合、制限に達するとボタンが無効化される（オプション）

### 8.4 席の表示
- [ ] 各テーブル内に複数の席が格子状に表示される
- [ ] 割り当てられた参加者の名前と番号が席に表示される
- [ ] 空席は "空席" またはグレーアウトで表示される

### 8.5 席のロック機能
- [ ] 参加者の席をタップすると、その席がロック状態（赤色のハイライト + 🔒 アイコン）に変わる
- [ ] ロック状態の席は、シャッフル時に移動しない
- [ ] ロック状態の席を再度タップするとロック解除される

### 8.6 テーブル編集
- [ ] テーブルをタップすると、テーブル編集シートが表示される
- [ ] 編集項目:
  - [ ] テーブル名の変更
  - [ ] 定員の増減（2～10人）
  - [ ] 横の列数の変更（1～4列）
  - [ ] 向き設定（正面・側面等、オプション）
- [ ] 変更を保存すると、テーブルがすぐに更新される
- [ ] テーブル削除ボタンがあり、削除するとテーブルがグリッドから消える

### 8.7 シャッフル（座席表側）
- [ ] ナビゲーションバー右上の「shuffle」アイコンをタップすると、座席配置が再シャッフル される
- [ ] ロック済みの席は動かない（保持される）
- [ ] ロック解除済みの席は新たに配置される

### 8.8 シェア機能
- [ ] ナビゲーションバー右上の「share」アイコンをタップすると、シェアシートが表示される
- [ ] 座席配置を含む席決め結果がテキスト形式で整理される
  - 期待: "テーブル1\n━━━\n🪑 [1列目・左] : 山田" 等が含まれる
- [ ] メッセージアプリ、メール、その他のアプリにシェアできる

### 8.9 レイアウト動作（列数・定員変更）
- [ ] テーブルの列数を変更すると、席の配置が即座に再配置される
- [ ] 定員を減らすと、超過分の参加者は別のテーブルに移動される（オプション）
  - テスト: 5人のテーブルの定員を3に変更 → 2人が他のテーブルに再配置

---

## 9. 番号札（SimpleShuffle）機能

### 9.1 番号札画面への遷移
- [ ] 参加者が複数いる場合、下部に「番号札」ボタンが表示される
- [ ] ボタンをタップすると番号札画面に遷移する
- [ ] 既にシャッフルされた状態で表示される

### 9.2 番号札表示
- [ ] 参加者がシャッフルされた順序で、リスト形式で表示される
- [ ] 各行に "1番席: 山田", "2番席: 鈴木" 等が表示される
- [ ] スクロール可能（参加者数が多い場合）

### 9.3 シャッフル（番号札側）
- [ ] ナビゲーションバー右上の「shuffle」ボタンをタップすると、番号札の順序が再シャッフルされる
- [ ] アニメーション（spring アニメーション）が付与される

### 9.4 シェア機能
- [ ] ナビゲーションバー右上の「share」ボタンをタップすると、番号札の順序がテキスト形式でシェアできる
  - 期待: "1番席: 山田\n2番席: 鈴木\n..." 等

---

## 10. 広告バナー機能（AdBanner）

### 10.1 広告表示
- [ ] すべてのメイン画面（参加者一覧・座席表・番号札）の下部に広告バナーが表示される
- [ ] インターネット接続がある場合、Google Mobile Ads から広告が読み込まれる
- [ ] インターネット接続がない場合でもアプリケーションが クラッシュしない（グレースフルフェイル）

### 10.2 広告のグレースフルフェイル
- [ ] ネットワークエラーが発生しても、アプリケーションの動作に支障がない
- [ ] 広告スペースが詰まることなく、余白として表示される（オプション）

---

## 11. ナビゲーション・画面遷移

### 11.1 戻る動作
- [ ] 座席表画面から「Back」または左スワイプで参加者一覧に戻る
- [ ] 番号札画面から「Back」または左スワイプで参加者一覧に戻る
- [ ] データは保持される（表示順序変更なし）

### 11.2 状態保持
- [ ] 座席表から参加者一覧に戻ると、参加者のシャッフル状態が保持される
- [ ] 番号札から戻ると、番号札の状態は参加者一覧に反映されない（独立している）

### 11.3 タブ・ナビゲーションバーの動作
- [ ] ナビゲーションバーのタイトルが各画面で適切に変更される
- [ ] 戻るボタンが適切に表示・非表示される

---

## 12. メモリ & パフォーマンス

### 12.1 大量の参加者追加
- [ ] 20名以上の参加者を追加してもアプリケーションが遅くならない
- [ ] リストのスクロール・シャッフルが滑らか（60fps）に動作する

### 12.2 メモリ リーク確認（グレード的）
- [ ] アプリケーションを起動・操作・戻る を複数回実施しても、メモリ増加が緩やかである
- [ ] Xcode の Memory Graph 等で大型オブジェクトが保持されていないことを確認（オプション）

### 12.3 応答性
- [ ] ボタン操作（追加・削除・シャッフル）の応答が 0.5 秒以内である
- [ ] 画面遷移がスムーズに動作する（遅延・フリーズがない）

---

## 13. エラーハンドリング & エッジケース

### 13.1 長い名前の入力
- [ ] 非常に長い名前（50文字以上）を入力しても、UI が崩れない
- [ ] 長い名前は TextField 内で適切に折り返し/スクロール される

### 13.2 特殊文字・絵文字の入力
- [ ] 絵文字を含む名前を追加できる
  - テスト: "山田😀" を入力
- [ ] 特殊文字（記号等）も正常に保存・表示される
  - テスト: "山田@!" を入力

### 13.3 言語・ローカライズ
- [ ] 日本語が正常に表示される
- [ ] 英語などの他言語を入力した場合も正常に表示される

### 13.4 入力フィールドの状態管理
- [ ] 一括追加シート表示中に、メイン画面の参加者一覧が更新されない（シートが閉じるまで）
- [ ] グループ読み込み時にアラート確認が必要な場合、スムーズに遷移する

---

## 14. ダーク・ライトモード対応

### 14.1 ライトモード表示
- [ ] デバイス設定で「ライトモード」に設定し、アプリケーションを起動
- [ ] すべての UI 要素が適切に表示される
- [ ] テキストの対比が十分であり読みやすい

### 14.2 ダークモード表示
- [ ] デバイス設定で「ダークモード」に設定し、アプリケーションを起動
- [ ] すべての UI 要素が適切に表示される
- [ ] テキストの対比が十分であり読みやすい

### 14.3 自動 / 手動切り替え
- [ ] システム設定でテーマを切り替えると、アプリケーション内もリアルタイムで反応する

---

## 15. デバイス・画面サイズ対応

### 15.1 標準サイズ iPhone
- [ ] iPhone 標準サイズ（例: iPhone 15, iPhone 16 の通常サイズ）で正常に動作
- [ ] すべてのボタンが押しやすい大きさ
- [ ] テキストが読みやすい大きさ

### 15.2 大型 iPhone
- [ ] iPhone Plus / Pro Max サイズで正常に動作
- [ ] UI が過度に拡大されていない（バランスが取れている）

### 15.3 iPad（オプション）
- [ ] iPad でアプリケーションを起動した場合も動作する（クラッシュしない）
- [ ] ただし、iPad 向け最適化は未実装でも良い

### 15.4 画面向きの変更
- [ ] 縦持ちで操作後、デバイスを横向きに回転させる
- [ ] 後方の横向きで UI が自動調整される（サイズ・レイアウト）
- [ ] 状態は保持される（参加者リストが初期化されない）

---

## 16. その他・総合的な確認

### 16.1 アプリ終了・復帰
- [ ] アプリケーションを起動し、参加者を追加・シャッフル後、ホーム画面に戻す
- [ ] アプリケーションアイコンをタップして復帰
- [ ] 前回の状態は保持されていない（通常、メモリ内のみなので初期化は想定）
  - リモートの持続化が実装されていない場合

### 16.2 バックグラウンド & フォアグラウンド
- [ ] アプリケーション起動中に別のアプリに切り替える
- [ ] 再度 SakuttoSeat にフォーカスを戻す
- [ ] 状態が正常に復帰する（UI が更新されている）

### 16.3 通知・アラート
- [ ] リセット確認アラート、グループ保存アラートが正しく表示される
- [ ] アラートのボタン（保存・キャンセル）が正確に機能する

### 16.4 キーボード操作
- [ ] TextField にフォーカスがある場合、ソフトキーボードが自動表示
- [ ] キーボードの return キー（submit）で参加者追加が実行される
- [ ] キーボード外の領域をタップすると、キーボードが隠れる

---

## 17. 試験環境・設定

| 項目 | 内容 |
|------|------|
| デバイス | iPhone __________ |
| iOS バージョン | iOS __________ |
| アプリダウンロード方法 | TestFlight / ビルドから直接インストール |
| 試験実施日 | __________ |
| 試験実施者 | __________ |
| 試験環境 | WiFi / キャリア回線 / オフライン（広告除外） |

---

## 18. 試験結果報告

### 総括
- [ ] **合格**：すべてのテスト項目に合格
- [ ] **合格（軽微）**：軽微な UI 不具合あり（記載）
- [ ] **再試**：重大な不具合が見つかった（詳細は下記）

### 不具合報告（該当する場合）

| No | 項目 | 現象 | 原因推定 | 優先度 | 対応 |
|-----|------|------|---------|--------|------|
| 1 |  |  |  | 🔴/🟡/🟢 |  |
| 2 |  |  |  | 🔴/🟡/🟢 |  |
| 3 |  |  |  | 🔴/🟡/🟢 |  |

**凡例**: 🔴: 高（リリース前に要修正）/ 🟡: 中（次バージョン）/ 🟢: 低（既知・許容）

### その他メモ

```
（自由記入欄）
実施者の感想、改善提案、テスト過程での気づきなど
```

---

## 19. テスト実施後のチェックリスト

- [ ] チェックリストを記入完了
- [ ] 重大な不具合が見つかった場合は、開発チームに報告
- [ ] 不具合の修正版で再試験を実施
- [ ] パスした場合、ビルド番号（またはリリース候補版）を記録
- [ ] リリース決定前に PO/PM の承認を得る

---

**附録: よく出るトラブルシューティング**

| 問題 | 対処法 |
|------|--------|
| アプリがクラッシュ | Xcode の Console を確認。メモリ不足やスタックトレースを記録 |
| 広告が読み込めない | ネットワーク接続確認。App Tracking Transparency 設定確認 |
| 状態がリセットされる | SwiftData がメモリ内保持のみの場合は仕様。永続化未実装 |
| UI が崩れている | 画面サイズ確認。Xcode の Canvas で表示確認 |
| キーボードが隠れない | 手動で dismiss する必要がある場合あり。仕様確認 |

---

**チェックリスト版番号**: v1.0  
**最終更新**: 2026年8月11日
